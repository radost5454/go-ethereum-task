name: Deploy Contracts to Devnet

on:
  workflow_dispatch:

jobs:
  deploy_contracts:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Checkout code
        uses: actions/checkout@v3

      - name: 🚀 Start Geth devnet container
        run: |
          docker run -d --name geth-devnet -p 8545:8545 \
            ghcr.io/${{ github.repository_owner }}/go-ethereum:dev-latest \
            geth \
              --dev \
              --datadir /tmp/devdatadir \
              --http --http.addr 0.0.0.0 --http.port 8545 \
              --http.api eth,net,web3,personal --allow-insecure-unlock

      - name: ⏳ Wait for Geth RPC
        run: |
          for i in {1..10}; do
            if curl -s http://localhost:8545 > /dev/null; then
              echo "✅ Geth RPC is up!"
              break
            fi
            echo "⏳ Still waiting for Geth RPC..."
            sleep 3
          done
          curl -s http://localhost:8545 || (echo "❌ Geth RPC did not respond" && exit 1)

      - name: 🧾 Log accounts and balances
        run: |
          docker exec geth-devnet geth --exec "eth.accounts.map(a => [a, eth.getBalance(a)])" attach http://localhost:8545


      - name: 🧪 Check Hardhat directory structure
        run: ls -R hardhat

      - name: 📦 Install Hardhat dependencies
        working-directory: hardhat
        run: |
          npm install

      - name: 👤 Check active signer
        working-directory: hardhat
        run: npx hardhat accounts:get --network geth



      - name: 🔨 Compile contracts
        working-directory: hardhat
        run: |
          npx hardhat compile

      - name: ✅ Run Hardhat unit tests (Waffle + Mocha)
        working-directory: hardhat
        run: |
          npx hardhat test

      - name: 💸 Fund the actual deployer address from Geth dev account
        run: |
          docker exec geth-devnet geth --exec "eth.sendTransaction({
            from: eth.accounts[0],
            to: '0xe260c22167D6e68FDc9d465C3751c1224a3962D3',
            value: web3.toWei(100, 'ether')
          })" attach http://localhost:8545


      - name: 🚀 Deploy contracts to Geth devnet
        working-directory: hardhat
        run: |
          npx hardhat run scripts/deploy.js --network geth

      - name: 🔍 Check chain ID
        run: |
          curl -s -X POST http://localhost:8545 \
            -H "Content-Type: application/json" \
            -d '{"jsonrpc":"2.0","method":"eth_chainId","params":[],"id":1}'

      - name: 📸 Commit container as new image with contracts
        run: |
          docker commit geth-devnet ghcr.io/${{ github.repository_owner }}/go-ethereum:devnet-with-contracts

      - name: 🔐 Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 📤 Push updated Geth image
        run: |
          docker push ghcr.io/${{ github.repository_owner }}/go-ethereum:devnet-with-contracts
